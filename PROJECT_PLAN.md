# BlogTwin 프로젝트 계획서

## 📋 목차
1. [프로젝트 개요](#프로젝트-개요)
2. [화면 기획](#화면-기획)
3. [기술 스택](#기술-스택)
4. [시스템 아키텍처](#시스템-아키텍처)
5. [세부 프로세스](#세부-프로세스)
6. [개발 로드맵](#개발-로드맵)
7. [API 명세](#api-명세)
8. [데이터베이스 설계](#데이터베이스-설계)
9. [보안 및 개인정보](#보안-및-개인정보)
10. [배포 전략](#배포-전략)

## 📚 추가 가이드 문서
- **[네이버 OAuth 연동 가이드](./NAVER_OAUTH_GUIDE.md)** - 딥링크 기반 네이버 로그인 완전 가이드

---

## 프로젝트 개요

### 프로젝트명
**BlogTwin** - AI 기반 개인 블로그 스타일 학습 및 자동 포스팅 시스템

### 목표
사용자의 기존 블로그 글을 분석하여 고유한 어투와 스타일을 학습하고, 새로운 주제나 사진을 입력받아 사용자의 스타일을 따라한 블로그 글을 자동 생성하는 네이티브 모바일 애플리케이션

### 핵심 기능
1. **블로그 연동 및 데이터 수집**
   - 최근 2개월 간의 블로그 글 자동 수집
   - 티스토리, 네이버 블로그 지원

2. **AI 스타일 학습**
   - 사용자의 어투, 문장 구조, 표현 방식 분석
   - 카테고리별 작성 패턴 학습
   - GPT-4를 활용한 스타일 프로파일 생성

3. **카테고리별 글 작성**
   - 주제/키워드 입력 시 학습된 스타일로 글 생성
   - 카테고리별 맞춤 콘텐츠 생성

4. **사진 기반 포스팅**
   - 사진 업로드 시 자동 분석
   - 사진 내용을 바탕으로 스토리텔링 생성
   - 사용자 스타일 적용

### 타겟 사용자
- 블로그를 운영하지만 시간이 부족한 사람
- 일관된 포스팅 스타일을 유지하고 싶은 블로거
- 여행, 일상 사진을 빠르게 블로그에 올리고 싶은 사용자

---

## 화면 기획

### 1. Splash & Onboarding
```
[Splash Screen]
- 앱 로고 및 브랜딩
- 로딩 애니메이션
- 초기 데이터 로드

[Onboarding Carousel - 3 Screens]
Screen 1: "당신의 블로그 스타일을 학습합니다"
Screen 2: "사진과 주제만으로 글을 작성합니다"
Screen 3: "언제 어디서나 빠른 포스팅"

[Get Started Button]
```

### 2. 블로그 연동 화면
```
┌─────────────────────────────────┐
│  BlogTwin                    [?]│
├─────────────────────────────────┤
│                                 │
│  블로그를 연동해주세요             │
│                                 │
│  ┌─────────────────────────┐   │
│  │  🅣  티스토리             │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │  N  네이버 블로그         │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │  V  Velog               │   │
│  └─────────────────────────┘   │
│                                 │
│  나중에 하기                     │
└─────────────────────────────────┘
```

#### 연동 프로세스
1. 블로그 플랫폼 선택
2. OAuth 인증 또는 API 키 입력
3. 권한 승인
4. 글 목록 불러오기 (Progress Bar 표시)
5. 스타일 분석 시작

### 3. 메인 대시보드
```
┌─────────────────────────────────┐
│  ☰  BlogTwin            🔔 ⚙️  │
├─────────────────────────────────┤
│  연동된 블로그: 티스토리           │
│  📊 분석된 글: 28개 (최근 2개월)  │
│  🕐 마지막 동기화: 2시간 전        │
│                                 │
│  ┌─────────────────────────┐   │
│  │   ✍️  카테고리별 글 작성   │   │
│  │   주제를 입력하고           │
│  │   AI가 글을 작성합니다      │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │   📸  사진으로 포스팅      │   │
│  │   사진만 올리면            │
│  │   자동으로 글이 작성됩니다   │
│  └─────────────────────────┘   │
│                                 │
│  ──────────────────────────    │
│  최근 작성 글                    │
│                                 │
│  📄 제주도 여행 후기 (임시저장)   │
│     30분 전                     │
│                                 │
│  ✅ 맛집 리뷰: 성수동 카페 (발행)│
│     2일 전                      │
└─────────────────────────────────┘
```

### 4. 카테고리별 글 작성 화면
```
┌─────────────────────────────────┐
│  ← 카테고리별 글 작성          ⋮ │
├─────────────────────────────────┤
│                                 │
│  카테고리                        │
│  [여행 ▼]                       │
│                                 │
│  주제/키워드                     │
│  ┌─────────────────────────┐   │
│  │ 예: 제주도 카페 투어      │   │
│  └─────────────────────────┘   │
│                                 │
│  목표 글자 수                    │
│  ●────────○────────            │
│  500        1500      3000      │
│                                 │
│  스타일 적용                     │
│  내 스타일 ●──────────○ 표준    │
│            80%                  │
│                                 │
│  추가 옵션                       │
│  ☑️ 이모지 사용                 │
│  ☐ 해시태그 자동 생성            │
│                                 │
│  ┌─────────────────────────┐   │
│  │      AI 글 작성 시작      │   │
│  └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

#### 글 생성 진행 화면
```
┌─────────────────────────────────┐
│  ← 글 작성 중...              ⋮ │
├─────────────────────────────────┤
│                                 │
│  🤖 AI가 글을 작성하고 있습니다   │
│                                 │
│  ████████░░░░░░░░  60%         │
│                                 │
│  ✅ 주제 분석 완료               │
│  ✅ 콘텐츠 구조 생성 완료         │
│  🔄 본문 작성 중...             │
│  ⏳ 스타일 적용 대기 중           │
│                                 │
│  예상 완료 시간: 약 30초          │
│                                 │
│  [취소]                         │
│                                 │
└─────────────────────────────────┘
```

#### 글 미리보기 및 편집
```
┌─────────────────────────────────┐
│  ← 미리보기                  ⋮  │
├─────────────────────────────────┤
│  [📝 편집] [🔄 재생성] [✓ 발행] │
├─────────────────────────────────┤
│                                 │
│  제주도 숨은 카페 투어 🍰         │
│  ─────────────────────────     │
│                                 │
│  안녕하세요~ 오늘은 제주도에서    │
│  찾은 정말 예쁜 카페들을 소개해   │
│  드리려고 해요!                  │
│                                 │
│  첫 번째로 방문한 곳은...        │
│  (스크롤 가능)                   │
│                                 │
│  ─────────────────────────     │
│  카테고리: 여행                  │
│  태그: #제주도 #카페 #여행       │
│  글자 수: 1,247자                │
│                                 │
└─────────────────────────────────┘
```

### 5. 사진 포스팅 화면
```
┌─────────────────────────────────┐
│  ← 사진으로 포스팅            ⋮ │
├─────────────────────────────────┤
│                                 │
│  사진 선택 (최대 10장)           │
│  ┌─────┬─────┬─────┬─────┐    │
│  │ 📷  │     │     │  +  │    │
│  │     │     │     │     │    │
│  └─────┴─────┴─────┴─────┘    │
│                                 │
│  사진 설명 자동 생성              │
│  ● 켜기   ○ 끄기                │
│                                 │
│  카테고리                        │
│  [일상 ▼]                       │
│                                 │
│  글 길이                         │
│  ○ 짧게  ● 보통  ○ 길게         │
│  (300자) (1000자) (2000자)      │
│                                 │
│  추가 메모 (선택사항)             │
│  ┌─────────────────────────┐   │
│  │ 오늘 날씨가 정말 좋았어요  │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │      글 생성하기          │   │
│  └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

### 6. AI 스타일 학습 화면
```
┌─────────────────────────────────┐
│  ← AI 스타일 분석             ⋮ │
├─────────────────────────────────┤
│  [🔄 동기화]                    │
│                                 │
│  📊 분석 통계                    │
│  ┌─────────────────────────┐   │
│  │ 총 글 수: 28개            │   │
│  │ 분석 기간: 2개월           │   │
│  │ 평균 글자 수: 1,850자      │   │
│  │ 주 작성 빈도: 3.5회        │   │
│  └─────────────────────────┘   │
│                                 │
│  🎨 스타일 특징                  │
│  ┌─────────────────────────┐   │
│  │ • 친근하고 편안한 말투      │   │
│  │ • 이모지 자주 사용 (😊🌟)  │   │
│  │ • 짧은 문장 선호           │   │
│  │ • 개인적 경험 중심         │   │
│  └─────────────────────────┘   │
│                                 │
│  📁 카테고리별 분포              │
│  여행        ████████░░  15개  │
│  맛집        ██████░░░░   8개  │
│  일상        ████░░░░░░   5개  │
│                                 │
│  💬 자주 사용하는 표현            │
│  "정말 좋았어요", "추천드려요",   │
│  "다음에 또", "강추합니다"        │
│                                 │
│  📝 최근 분석한 글               │
│  • 속초 여행 브이로그 (2일 전)   │
│  • 홍대 신상 카페 후기 (5일 전)  │
│  (더보기...)                    │
│                                 │
└─────────────────────────────────┘
```

### 7. 글 편집기
```
┌─────────────────────────────────┐
│  ← 편집                    [저장]│
├─────────────────────────────────┤
│  [B] [I] [U] [🔗] [📷] [⚙️]    │
├─────────────────────────────────┤
│  제목                            │
│  ┌─────────────────────────┐   │
│  │ 제주도 숨은 카페 투어      │   │
│  └─────────────────────────┘   │
│                                 │
│  본문                            │
│  ┌─────────────────────────┐   │
│  │ 안녕하세요~ 오늘은...     │   │
│  │                         │   │
│  │ [이미지]                 │   │
│  │                         │   │
│  │ 첫 번째로 방문한 곳은...  │   │
│  │                         │   │
│  │ (편집 가능 영역)          │   │
│  └─────────────────────────┘   │
│                                 │
│  AI 어시스턴트                   │
│  [✨ 문장 다듬기] [➕ 문단 추가]│
│  [✓ 맞춤법 검사] [🎨 표현 개선]│
│                                 │
│  태그                            │
│  #제주도 #카페 #여행             │
│  + 태그 추가                     │
│                                 │
│  ┌─────────────────────────┐   │
│  │      임시저장 | 발행하기   │   │
│  └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

### 8. 발행 설정 화면
```
┌─────────────────────────────────┐
│  ← 발행 설정                  ✕ │
├─────────────────────────────────┤
│                                 │
│  발행 시간                       │
│  ● 즉시 발행                    │
│  ○ 예약 발행                    │
│                                 │
│  공개 설정                       │
│  ● 전체 공개                    │
│  ○ 비공개                       │
│                                 │
│  카테고리                        │
│  [여행 ▼]                       │
│                                 │
│  태그                            │
│  #제주도 #카페 #여행             │
│                                 │
│  댓글 허용                       │
│  ● 허용   ○ 비허용              │
│                                 │
│  ┌─────────────────────────┐   │
│  │        발행하기           │   │
│  └─────────────────────────┘   │
│                                 │
│  [취소]                         │
│                                 │
└─────────────────────────────────┘
```

### 9. 설정 화면
```
┌─────────────────────────────────┐
│  ← 설정                          │
├─────────────────────────────────┤
│                                 │
│  계정                            │
│  > 블로그 연동 관리               │
│  > OpenAI API 키 설정            │
│                                 │
│  ──────────────────────────    │
│  AI 설정                         │
│  > 학습 데이터 범위 (2개월)       │
│  > 기본 글 길이 (보통)            │
│  > 기본 카테고리 설정             │
│  > 자동 발행 (끔)                │
│                                 │
│  ──────────────────────────    │
│  앱 설정                         │
│  > 알림 설정                     │
│  > 테마 (라이트/다크)             │
│  > 언어                          │
│                                 │
│  ──────────────────────────    │
│  데이터                          │
│  > 캐시 삭제                     │
│  > 임시저장 글 관리               │
│                                 │
│  ──────────────────────────    │
│  정보                            │
│  > 앱 버전: 1.0.0                │
│  > 이용약관                      │
│  > 개인정보 처리방침              │
│  > 문의하기                      │
│                                 │
└─────────────────────────────────┘
```

---

## 기술 스택

### Frontend (Mobile App)

#### React Native (권장)
```json
{
  "framework": "React Native 0.73+",
  "language": "TypeScript",
  "ui": {
    "components": "React Native Paper / NativeBase",
    "navigation": "@react-navigation/native",
    "styling": "styled-components / Tailwind CSS (NativeWind)"
  },
  "state": {
    "global": "Zustand / Redux Toolkit",
    "server": "@tanstack/react-query",
    "form": "react-hook-form"
  },
  "storage": {
    "local": "@react-native-async-storage/async-storage",
    "database": "WatermelonDB / SQLite"
  },
  "imaging": {
    "picker": "react-native-image-picker",
    "crop": "react-native-image-crop-picker",
    "cache": "react-native-fast-image"
  },
  "editor": {
    "markdown": "react-native-markdown-editor",
    "rich-text": "@10play/tentap-editor"
  },
  "oauth": {
    "browser": "react-native-inappbrowser-reborn",
    "deeplink": "react-native built-in Linking"
  }
}
```

### Backend & Services

#### Firebase / Supabase
```yaml
services:
  authentication:
    - OAuth 2.0 (Google, Apple)
    - Email/Password

  database:
    - Firestore / PostgreSQL
    - Real-time sync

  storage:
    - Image hosting
    - User data backup

  functions:
    - Serverless API endpoints
    - Scheduled tasks (sync, cleanup)

  analytics:
    - User behavior tracking
    - Error monitoring (Sentry)
```

#### AI & External APIs
```yaml
ai_services:
  openai:
    - GPT-4 Turbo (text generation)
    - GPT-4 Vision (image analysis)
    - Embeddings (style learning)

blog_apis:
  tistory:
    - OAuth 2.0
    - REST API

  naver:
    - OAuth 2.0
    - API integration

  velog:
    - GraphQL API (if available)
```

### DevOps & Tools

```yaml
development:
  version_control: Git + GitHub
  ci_cd: GitHub Actions / Bitrise
  code_quality:
    - ESLint + Prettier
    - Husky (pre-commit hooks)
  testing:
    - Jest (unit tests)
    - Detox (E2E tests)

deployment:
  ios:
    - Xcode Cloud
    - TestFlight (beta)
    - App Store

  android:
    - Google Play Console
    - Internal testing track

monitoring:
  errors: Sentry
  analytics: Firebase Analytics / Mixpanel
  performance: Firebase Performance Monitoring
```

---

## 시스템 아키텍처

### 전체 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────┐
│                     Mobile App (React Native)            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  UI Layer    │  │ State Mgmt   │  │ Local Storage│  │
│  │  (Screens)   │  │  (Zustand)   │  │ (AsyncStorage│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│         │                  │                  │          │
│  ┌──────▼──────────────────▼──────────────────▼──────┐  │
│  │           Business Logic Layer                    │  │
│  │  • Blog Service  • AI Service  • Auth Service    │  │
│  └──────┬──────────────────┬──────────────────┬──────┘  │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          │                  │                  │
┌─────────▼─────┐  ┌─────────▼─────┐  ┌────────▼─────────┐
│ Blog APIs     │  │ OpenAI API    │  │ Backend Services │
│ • Tistory     │  │ • GPT-4       │  │ • Firebase/      │
│ • Naver Blog  │  │ • Vision      │  │   Supabase       │
│ • Velog       │  │ • Embeddings  │  │ • Auth           │
└───────────────┘  └───────────────┘  │ • Database       │
                                      │ • Storage        │
                                      │ • Functions      │
                                      └──────────────────┘
```

### 데이터 흐름

#### 1. 블로그 연동 및 스타일 학습
```
[사용자] → [블로그 선택] → [OAuth 인증]
    ↓
[Access Token 저장] → [글 목록 API 호출]
    ↓
[최근 2개월 글 수집] → [텍스트 전처리]
    ↓
[GPT-4 Embeddings 생성] → [스타일 특징 추출]
    ↓
[Firebase에 스타일 프로파일 저장] → [로컬 캐시 저장]
```

#### 2. 카테고리별 글 작성
```
[사용자 입력]
  - 카테고리
  - 주제/키워드
  - 옵션 (글자 수, 스타일 강도)
    ↓
[스타일 프로파일 로드] + [카테고리 데이터 로드]
    ↓
[프롬프트 생성]
  - 시스템 프롬프트 (스타일 가이드)
  - 사용자 프롬프트 (주제 + 요구사항)
    ↓
[GPT-4 API 호출] → [스트리밍 응답]
    ↓
[실시간 미리보기 업데이트]
    ↓
[완성된 글] → [로컬 임시 저장] → [편집/발행]
```

#### 3. 사진 기반 포스팅
```
[사진 선택] → [이미지 압축/최적화]
    ↓
[Firebase Storage 업로드]
    ↓
[GPT-4 Vision API]
  - 이미지 분석
  - 객체/장면 인식
  - 분위기 파악
    ↓
[이미지 설명 생성] + [스타일 프로파일]
    ↓
[프롬프트 조합]
    ↓
[GPT-4 텍스트 생성]
    ↓
[이미지 + 텍스트 결합] → [미리보기] → [편집/발행]
```

#### 4. 글 발행
```
[발행 버튼 클릭] → [발행 설정 확인]
    ↓
[블로그 API 토큰 검증]
    ↓
[이미지 업로드] (블로그 서버)
    ↓
[HTML/마크다운 변환]
    ↓
[블로그 포스트 API 호출]
  - 제목, 본문, 카테고리, 태그
    ↓
[발행 성공] → [히스토리 저장] → [알림]
```

---

## 세부 프로세스

### 1. 블로그 API 연동 프로세스

#### 1.1 티스토리 연동
```typescript
// 프로세스 단계
1. OAuth 인증 플로우
   - Redirect URI: blogtwin://oauth/tistory
   - Scope: read, write

2. Access Token 획득
   POST https://www.tistory.com/oauth/access_token
   {
     client_id: string,
     client_secret: string,
     redirect_uri: string,
     code: string,
     grant_type: "authorization_code"
   }

3. 블로그 정보 조회
   GET https://www.tistory.com/apis/blog/info
   ?access_token={token}&output=json

4. 글 목록 조회 (페이징)
   GET https://www.tistory.com/apis/post/list
   ?access_token={token}
   &blogName={name}
   &page={page}
   &count=30

5. 개별 글 상세 조회
   GET https://www.tistory.com/apis/post/read
   ?access_token={token}
   &blogName={name}
   &postId={id}

6. 글 발행
   POST https://www.tistory.com/apis/post/write
   {
     access_token: string,
     blogName: string,
     title: string,
     content: string,
     category: string,
     tag: string,
     visibility: 0 | 1 | 2, // 비공개/보호/공개
   }
```

#### 1.2 네이버 블로그 연동
```typescript
// ✅ 네이티브 앱에서 완전히 가능! (딥링크 기반 OAuth)

1. OAuth 인증 시작 (앱 내에서)
   - "네이버 로그인" 버튼 탭
   - InAppBrowser로 네이버 로그인 화면 오픈
   - URL: https://nid.naver.com/oauth2.0/authorize
     ?client_id={CLIENT_ID}
     &redirect_uri=blogtwin://oauth/naver  // 딥링크!
     &response_type=code
     &state={RANDOM_STATE}

2. 사용자 로그인 (네이버 앱 or 인앱 브라우저)
   - 네이버 앱 설치 시 → 네이버 앱으로 전환
   - 미설치 시 → Custom Tabs(Android) / SFSafariViewController(iOS)
   - 사용자가 로그인 + 동의

3. Redirect → 앱으로 복귀
   - 네이버가 호출: blogtwin://oauth/naver?code=ABC123&state=xyz
   - 앱의 딥링크 핸들러가 받음 (Linking.addEventListener)

4. Code → Access Token 교환 (서버에서)
   - 앱 → Firebase Functions에 code 전달
   - Functions → 네이버 API 호출
   POST https://nid.naver.com/oauth2.0/token
   {
     grant_type: 'authorization_code',
     client_id: CLIENT_ID,
     client_secret: CLIENT_SECRET,  // 서버에서만 사용!
     code: code
   }

5. 토큰 저장
   - Firestore에 암호화하여 저장
   - 앱에 "연동 완료" 표시

6. 블로그 글 목록 가져오기
   GET https://openapi.naver.com/v1/search/blog.json
   Headers: { Authorization: `Bearer ${access_token}` }

⚠️ 주의사항:
- ID/비밀번호를 직접 받는 건 절대 금지!
- Client Secret은 서버에서만 사용
- 딥링크 스킴(blogtwin://)을 AndroidManifest.xml, Info.plist에 등록 필수

📖 상세 가이드: NAVER_OAUTH_GUIDE.md 참조
```

### 2. AI 스타일 학습 프로세스

#### 2.1 데이터 수집 및 전처리
```python
# 의사코드
def collect_and_preprocess_posts(blog_posts):
    """
    블로그 글 수집 및 전처리
    """
    processed_data = []

    for post in blog_posts:
        # HTML 태그 제거
        clean_text = strip_html(post.content)

        # 문장 분리
        sentences = split_sentences(clean_text)

        # 특수문자, 이모지 분석
        emojis = extract_emojis(clean_text)
        special_chars = extract_special_chars(clean_text)

        # 문장 길이 분석
        sentence_lengths = [len(s) for s in sentences]

        # 단락 구조 분석
        paragraphs = split_paragraphs(clean_text)

        processed_data.append({
            'post_id': post.id,
            'category': post.category,
            'title': post.title,
            'content': clean_text,
            'sentences': sentences,
            'sentence_count': len(sentences),
            'avg_sentence_length': mean(sentence_lengths),
            'paragraph_count': len(paragraphs),
            'emojis': emojis,
            'emoji_count': len(emojis),
            'word_count': len(clean_text.split()),
            'published_date': post.date
        })

    return processed_data
```

#### 2.2 스타일 특징 추출
```python
def extract_style_features(processed_posts):
    """
    GPT-4를 활용한 스타일 특징 추출
    """
    # 1. 통계적 특징
    statistics = {
        'avg_post_length': mean([p['word_count'] for p in processed_posts]),
        'avg_sentence_length': mean([p['avg_sentence_length'] for p in processed_posts]),
        'emoji_usage_rate': sum([p['emoji_count'] for p in processed_posts]) / len(processed_posts),
        'posting_frequency': calculate_frequency(processed_posts)
    }

    # 2. 언어적 특징 (GPT-4 분석)
    style_prompt = f"""
    다음 블로그 글들을 분석하여 작성자의 스타일 특징을 추출해주세요:

    글 샘플:
    {format_posts_for_analysis(processed_posts[:10])}

    다음 항목을 분석해주세요:
    1. 어투 (존댓말/반말, 격식/비격식)
    2. 자주 사용하는 표현 (10개)
    3. 문장 구조 패턴
    4. 글의 전개 방식 (서론-본론-결론 등)
    5. 특징적인 어휘나 표현
    6. 이모지/특수문자 사용 패턴
    """

    linguistic_features = call_gpt4(style_prompt)

    # 3. 카테고리별 특징
    category_styles = {}
    for category in set([p['category'] for p in processed_posts]):
        category_posts = [p for p in processed_posts if p['category'] == category]
        category_styles[category] = analyze_category_style(category_posts)

    return {
        'statistics': statistics,
        'linguistic_features': linguistic_features,
        'category_styles': category_styles,
        'sample_posts': processed_posts[:5]  # 참고용
    }
```

#### 2.3 스타일 프로파일 생성
```typescript
interface StyleProfile {
  userId: string;
  blogPlatform: 'tistory' | 'naver' | 'velog';

  // 통계 정보
  statistics: {
    totalPosts: number;
    avgPostLength: number;
    avgSentenceLength: number;
    emojiUsageRate: number;
    postingFrequency: number;
  };

  // 언어 스타일
  linguisticStyle: {
    tone: 'formal' | 'casual' | 'friendly';
    perspective: '1st_person' | '3rd_person';
    commonPhrases: string[];  // ["정말 좋았어요", "추천드려요"]
    sentencePatterns: string[];
    vocabularyLevel: 'simple' | 'moderate' | 'advanced';
  };

  // 구조 패턴
  structuralPattern: {
    introStyle: string;  // "인사로 시작", "질문으로 시작"
    bodyDevelopment: string;  // "시간순", "주제별"
    conclusionStyle: string;  // "요약", "다짐", "질문"
    avgParagraphCount: number;
  };

  // 카테고리별 스타일
  categoryStyles: {
    [category: string]: {
      samplePosts: string[];
      specificPhrases: string[];
      avgLength: number;
    };
  };

  // GPT 프롬프트용 요약
  styleGuide: string;  // GPT에게 전달할 스타일 가이드 텍스트

  // 메타데이터
  lastUpdated: Date;
  analyzedPostCount: number;
  dataRange: { from: Date; to: Date };
}
```

#### 2.4 스타일 가이드 생성 (GPT 프롬프트)
```typescript
function generateStyleGuide(styleProfile: StyleProfile): string {
  return `
당신은 블로그 작성자의 스타일을 완벽하게 모방하는 AI입니다.

## 작성자 스타일 특징

### 어투 및 톤
- ${styleProfile.linguisticStyle.tone} 톤 사용
- ${styleProfile.linguisticStyle.perspective} 시점으로 작성

### 자주 사용하는 표현
${styleProfile.linguisticStyle.commonPhrases.map(p => `- "${p}"`).join('\n')}

### 문장 구조
- 평균 문장 길이: ${styleProfile.statistics.avgSentenceLength}자
- 문장 패턴: ${styleProfile.linguisticStyle.sentencePatterns.join(', ')}

### 글 구조
- 서론: ${styleProfile.structuralPattern.introStyle}
- 본론: ${styleProfile.structuralPattern.bodyDevelopment}
- 결론: ${styleProfile.structuralPattern.conclusionStyle}
- 평균 문단 수: ${styleProfile.structuralPattern.avgParagraphCount}개

### 이모지 사용
- 사용 빈도: ${styleProfile.statistics.emojiUsageRate}회/글
- 패턴: ${styleProfile.statistics.emojiUsageRate > 3 ? '적극적으로 사용' : '절제된 사용'}

### 참고 글 샘플
${styleProfile.categoryStyles['여행']?.samplePosts[0] || ''}

위 스타일을 정확히 따라 글을 작성해주세요.
  `.trim();
}
```

### 3. 글 생성 프로세스

#### 3.1 카테고리별 글 생성
```typescript
async function generateCategoryPost(
  topic: string,
  category: string,
  options: {
    targetLength: number;
    styleIntensity: number;  // 0-100
    useEmoji: boolean;
    autoHashtag: boolean;
  },
  styleProfile: StyleProfile
): Promise<GeneratedPost> {

  // 1. 카테고리별 스타일 로드
  const categoryStyle = styleProfile.categoryStyles[category];

  // 2. 시스템 프롬프트 구성
  const systemPrompt = `
${generateStyleGuide(styleProfile)}

현재 작성할 카테고리: ${category}
이 카테고리의 특징:
${categoryStyle?.specificPhrases.join(', ')}

스타일 적용 강도: ${options.styleIntensity}%
(100%에 가까울수록 작성자의 스타일을 더 강하게 모방)
  `;

  // 3. 사용자 프롬프트 구성
  const userPrompt = `
주제: ${topic}

다음 조건으로 블로그 글을 작성해주세요:
- 목표 길이: 약 ${options.targetLength}자
- 이모지 사용: ${options.useEmoji ? '예' : '아니오'}
- 카테고리: ${category}

글의 구조:
1. 흥미로운 서론
2. 구체적이고 상세한 본론
3. 자연스러운 마무리

${options.autoHashtag ? '글 마지막에 적절한 해시태그 3-5개를 추가해주세요.' : ''}
  `;

  // 4. GPT-4 API 호출 (스트리밍)
  const stream = await openai.chat.completions.create({
    model: 'gpt-4-turbo-preview',
    messages: [
      { role: 'system', content: systemPrompt },
      { role: 'user', content: userPrompt }
    ],
    temperature: 0.7,
    max_tokens: Math.ceil(options.targetLength * 1.5),
    stream: true
  });

  // 5. 스트리밍 응답 처리
  let generatedContent = '';
  for await (const chunk of stream) {
    const content = chunk.choices[0]?.delta?.content || '';
    generatedContent += content;

    // 실시간 UI 업데이트
    emitProgress({
      type: 'content_update',
      content: generatedContent,
      progress: estimateProgress(generatedContent, options.targetLength)
    });
  }

  // 6. 후처리
  const processed = postProcess(generatedContent, {
    extractTitle: true,
    extractHashtags: options.autoHashtag,
    formatMarkdown: true
  });

  return {
    title: processed.title,
    content: processed.content,
    hashtags: processed.hashtags,
    category: category,
    wordCount: processed.wordCount,
    metadata: {
      generatedAt: new Date(),
      topic: topic,
      model: 'gpt-4-turbo'
    }
  };
}
```

#### 3.2 사진 기반 글 생성
```typescript
async function generatePhotoPost(
  images: ImageFile[],
  category: string,
  options: {
    length: 'short' | 'medium' | 'long';
    autoDescription: boolean;
    additionalNote?: string;
  },
  styleProfile: StyleProfile
): Promise<GeneratedPost> {

  // 1. 이미지 업로드 및 URL 생성
  const uploadedImages = await Promise.all(
    images.map(img => uploadToStorage(img))
  );

  // 2. GPT-4 Vision으로 이미지 분석
  const imageAnalyses = await Promise.all(
    uploadedImages.map(async (img) => {
      const analysis = await openai.chat.completions.create({
        model: 'gpt-4-vision-preview',
        messages: [
          {
            role: 'user',
            content: [
              {
                type: 'text',
                text: `이 이미지를 분석하여 다음을 설명해주세요:
1. 주요 객체/인물
2. 장소/배경
3. 분위기/느낌
4. 특별한 점
5. 이미지가 전달하는 스토리`
              },
              {
                type: 'image_url',
                image_url: { url: img.url }
              }
            ]
          }
        ],
        max_tokens: 500
      });

      return {
        url: img.url,
        description: analysis.choices[0].message.content
      };
    })
  );

  // 3. 이미지 설명을 바탕으로 글 구성
  const imageContext = imageAnalyses
    .map((img, idx) => `사진 ${idx + 1}: ${img.description}`)
    .join('\n\n');

  // 4. 글 길이 설정
  const targetLengths = {
    short: 500,
    medium: 1200,
    long: 2500
  };

  // 5. 프롬프트 구성
  const systemPrompt = generateStyleGuide(styleProfile);

  const userPrompt = `
다음 사진들을 바탕으로 블로그 글을 작성해주세요:

${imageContext}

${options.additionalNote ? `작성자 메모: ${options.additionalNote}` : ''}

요구사항:
- 카테고리: ${category}
- 목표 길이: 약 ${targetLengths[options.length]}자
- ${imageAnalyses.length}장의 사진이 있으며, 각 사진을 적절히 언급
- 사진의 순서대로 이야기를 전개
- 자연스러운 스토리텔링

글 구조:
1. 흥미로운 시작 (왜 이곳에 갔는지, 어떤 상황인지)
2. 사진별 설명과 에피소드
3. 전체적인 느낌과 마무리

[사진1], [사진2] 형식으로 사진이 들어갈 위치를 표시해주세요.
  `;

  // 6. 글 생성
  const completion = await openai.chat.completions.create({
    model: 'gpt-4-turbo-preview',
    messages: [
      { role: 'system', content: systemPrompt },
      { role: 'user', content: userPrompt }
    ],
    temperature: 0.8,
    max_tokens: targetLengths[options.length] * 2
  });

  let content = completion.choices[0].message.content;

  // 7. 이미지 마커를 실제 이미지로 교체
  imageAnalyses.forEach((img, idx) => {
    const marker = `[사진${idx + 1}]`;
    const imageHtml = `<img src="${img.url}" alt="사진 ${idx + 1}" />`;
    content = content.replace(marker, imageHtml);
  });

  // 8. 제목 및 해시태그 추출
  const processed = postProcess(content, {
    extractTitle: true,
    extractHashtags: true,
    formatMarkdown: true
  });

  return {
    title: processed.title,
    content: processed.content,
    images: uploadedImages,
    hashtags: processed.hashtags,
    category: category,
    metadata: {
      generatedAt: new Date(),
      imageCount: images.length,
      model: 'gpt-4-vision + gpt-4-turbo'
    }
  };
}
```

### 4. 발행 프로세스

```typescript
async function publishPost(
  post: GeneratedPost,
  settings: {
    publishTime: 'now' | Date;
    visibility: 'public' | 'private';
    allowComments: boolean;
  },
  blogConnection: BlogConnection
): Promise<PublishResult> {

  try {
    // 1. 토큰 유효성 검증
    const isValid = await validateToken(blogConnection.accessToken);
    if (!isValid) {
      // 토큰 갱신 시도
      blogConnection.accessToken = await refreshToken(blogConnection.refreshToken);
    }

    // 2. 이미지 처리 (로컬 → 블로그 서버)
    const processedImages = await Promise.all(
      post.images?.map(async (img) => {
        // 블로그 서버에 이미지 업로드
        const uploaded = await uploadImageToBlog(img, blogConnection);
        return uploaded.url;
      }) || []
    );

    // 3. 콘텐츠 변환 (마크다운 → HTML)
    let htmlContent = markdownToHtml(post.content);

    // 이미지 URL 교체
    post.images?.forEach((img, idx) => {
      htmlContent = htmlContent.replace(img.url, processedImages[idx]);
    });

    // 4. 플랫폼별 API 호출
    let publishResult;

    switch (blogConnection.platform) {
      case 'tistory':
        publishResult = await publishToTistory({
          accessToken: blogConnection.accessToken,
          blogName: blogConnection.blogName,
          title: post.title,
          content: htmlContent,
          category: post.category,
          tag: post.hashtags?.join(','),
          visibility: settings.visibility === 'public' ? 3 : 0,
          acceptComment: settings.allowComments ? 1 : 0,
          publishTime: settings.publishTime === 'now'
            ? undefined
            : Math.floor(settings.publishTime.getTime() / 1000)
        });
        break;

      case 'naver':
        publishResult = await publishToNaver({
          // 네이버 API 파라미터
        });
        break;

      case 'velog':
        publishResult = await publishToVelog({
          // Velog API 파라미터
        });
        break;
    }

    // 5. 발행 기록 저장
    await savePublishHistory({
      postId: publishResult.postId,
      postUrl: publishResult.url,
      publishedAt: new Date(),
      platform: blogConnection.platform,
      title: post.title,
      category: post.category,
      wordCount: post.wordCount
    });

    // 6. 로컬 임시저장 글 삭제
    await deleteLocalDraft(post.id);

    // 7. 성공 알림
    await sendNotification({
      title: '발행 완료',
      body: `"${post.title}" 글이 성공적으로 발행되었습니다.`,
      data: { url: publishResult.url }
    });

    return {
      success: true,
      postId: publishResult.postId,
      url: publishResult.url
    };

  } catch (error) {
    // 에러 처리
    console.error('Publishing failed:', error);

    // 에러 알림
    await sendNotification({
      title: '발행 실패',
      body: error.message || '글 발행 중 오류가 발생했습니다.'
    });

    return {
      success: false,
      error: error.message
    };
  }
}
```

### 5. 백그라운드 동기화 프로세스

```typescript
// 정기적으로 블로그 글을 동기화하여 스타일 학습 데이터 최신화
async function backgroundSync() {
  // 1. 마지막 동기화 시간 확인
  const lastSync = await getLastSyncTime();
  const now = new Date();

  // 2. 24시간 이상 지났으면 동기화 실행
  if (now.getTime() - lastSync.getTime() < 24 * 60 * 60 * 1000) {
    return;
  }

  // 3. 새로운 글 가져오기
  const newPosts = await fetchNewPosts(lastSync);

  if (newPosts.length === 0) {
    await updateLastSyncTime(now);
    return;
  }

  // 4. 스타일 재분석
  const allPosts = await getAllPosts();
  const updatedPosts = [...allPosts, ...newPosts];

  const newStyleProfile = await analyzeAndBuildStyleProfile(updatedPosts);

  // 5. 스타일 프로파일 업데이트
  await updateStyleProfile(newStyleProfile);

  // 6. 동기화 시간 업데이트
  await updateLastSyncTime(now);

  // 7. 알림
  await sendNotification({
    title: '스타일 학습 업데이트',
    body: `${newPosts.length}개의 새 글이 분석되었습니다.`
  });
}

// Background task 등록 (React Native Background Fetch)
BackgroundFetch.configure({
  minimumFetchInterval: 24 * 60, // 24시간 (분 단위)
  stopOnTerminate: false,
  startOnBoot: true
}, async (taskId) => {
  await backgroundSync();
  BackgroundFetch.finish(taskId);
});
```

---

## 개발 로드맵

### Phase 1: 기반 구축 (4주)

#### Week 1: 프로젝트 설정 및 기본 UI
**목표**: 프로젝트 초기화 및 핵심 화면 UI 완성

**작업 내용**:
- [ ] React Native 프로젝트 생성
  ```bash
  npx react-native init BlogTwin --template react-native-template-typescript
  ```
- [ ] 필수 라이브러리 설치
  ```json
  {
    "dependencies": {
      "@react-navigation/native": "^6.1.9",
      "@react-navigation/stack": "^6.3.20",
      "react-native-paper": "^5.11.3",
      "zustand": "^4.4.7",
      "@tanstack/react-query": "^5.12.2",
      "react-hook-form": "^7.48.2"
    }
  }
  ```
- [ ] 폴더 구조 설계
  ```
  src/
  ├── screens/          # 화면 컴포넌트
  ├── components/       # 재사용 컴포넌트
  ├── services/         # API 서비스
  ├── stores/           # Zustand 스토어
  ├── types/            # TypeScript 타입
  ├── utils/            # 유틸리티 함수
  ├── hooks/            # 커스텀 훅
  └── constants/        # 상수
  ```
- [ ] 디자인 시스템 구축
  - 컬러 팔레트 정의
  - 타이포그래피 설정
  - 공통 컴포넌트 (Button, Input, Card 등)
- [ ] 네비게이션 구조 설정
  - Stack Navigator (온보딩, 연동, 메인)
  - Tab Navigator (메인 기능들)

**산출물**:
- 실행 가능한 기본 앱
- Splash, Onboarding, 메인 화면 UI

#### Week 2: Firebase 설정 및 인증
**목표**: 백엔드 인프라 구축 및 사용자 인증 구현

**작업 내용**:
- [ ] Firebase 프로젝트 생성
- [ ] Firebase SDK 통합
  ```bash
  npm install @react-native-firebase/app @react-native-firebase/auth
  ```
- [ ] 이메일/비밀번호 인증 구현
- [ ] 소셜 로그인 (Google, Apple) 구현
- [ ] 사용자 상태 관리 (Zustand)
- [ ] Firestore 데이터베이스 설계
  ```
  Collections:
  - users/{userId}
  - blogConnections/{userId}/connections/{connectionId}
  - stylProfiles/{userId}
  - posts/{userId}/drafts/{postId}
  - publishHistory/{userId}/published/{historyId}
  ```
- [ ] Firebase Storage 설정 (이미지 저장용)

**산출물**:
- 완성된 인증 시스템
- Firebase 인프라 구축

#### Week 3: 블로그 API 연동 - 티스토리
**목표**: 티스토리 OAuth 인증 및 기본 API 통합

**작업 내용**:
- [ ] 티스토리 앱 등록 (Client ID/Secret 발급)
- [ ] OAuth 인증 플로우 구현
  ```typescript
  // services/tistory.service.ts
  class TistoryService {
    async authenticate(): Promise<string>
    async getBlogInfo(token: string): Promise<BlogInfo>
    async fetchPosts(token: string, page: number): Promise<Post[]>
    async fetchPostDetail(token: string, postId: string): Promise<PostDetail>
    async publishPost(token: string, data: PostData): Promise<PublishResult>
  }
  ```
- [ ] WebView를 통한 OAuth 처리
- [ ] Access Token 저장 (암호화)
- [ ] 블로그 정보 조회 기능
- [ ] 글 목록 불러오기 (페이징)
- [ ] 글 상세 조회
- [ ] 에러 처리 및 토큰 갱신

**산출물**:
- 티스토리 연동 완료
- 블로그 글 불러오기 기능

#### Week 4: 블로그 API 연동 - 네이버 & Velog
**목표**: 다중 플랫폼 지원

**작업 내용**:
- [ ] 네이버 블로그 OAuth 연동 (딥링크 기반)
  - react-native-inappbrowser-reborn 설치 및 설정
  - 딥링크 스킴 설정 (blogtwin://oauth/naver)
    - AndroidManifest.xml에 intent-filter 추가
    - iOS Info.plist에 CFBundleURLSchemes 추가
  - NaverOAuthService 클래스 구현
  - Firebase Functions에 토큰 교환 함수 작성
  - State 검증 (CSRF 방지) 구현
  - 토큰 암호화 저장
  - 네이버 프로필 API 연동
  - 블로그 정보 조회 (검색 API)
- [ ] Velog API 연동 (가능한 경우)
  - GraphQL API 분석
  - 토큰 기반 인증
- [ ] 통합 BlogService 인터페이스
  ```typescript
  interface IBlogService {
    authenticate(): Promise<AuthResult>;
    fetchPosts(params: FetchParams): Promise<Post[]>;
    publishPost(post: PostData): Promise<PublishResult>;
  }

  class BlogServiceFactory {
    static create(platform: BlogPlatform): IBlogService
  }
  ```
- [ ] 플랫폼별 데이터 정규화
- [ ] 연동 관리 화면 구현

**산출물**:
- 3개 플랫폼 연동 완료
- 통합 API 서비스

---

### Phase 2: AI 핵심 기능 (4주)

#### Week 5: OpenAI API 통합
**목표**: GPT-4 API 연동 및 기본 텍스트 생성

**작업 내용**:
- [ ] OpenAI API 키 설정 시스템
  ```typescript
  // services/openai.service.ts
  class OpenAIService {
    private apiKey: string;

    async chat(messages: Message[], options?: ChatOptions): Promise<string>
    async chatStream(messages: Message[], onChunk: (text: string) => void): Promise<void>
    async analyzeImage(imageUrl: string, prompt: string): Promise<string>
    async generateEmbedding(text: string): Promise<number[]>
  }
  ```
- [ ] GPT-4 Turbo 통합
- [ ] GPT-4 Vision 통합
- [ ] 스트리밍 응답 처리
- [ ] 요청/응답 로깅 시스템
- [ ] 비용 추적 시스템
- [ ] 에러 처리 및 재시도 로직
- [ ] Rate limiting 처리

**산출물**:
- OpenAI API 서비스 완성
- 기본 텍스트 생성 테스트

#### Week 6: 스타일 분석 시스템
**목표**: 블로그 글 분석 및 스타일 특징 추출

**작업 내용**:
- [ ] 텍스트 전처리 유틸리티
  ```typescript
  // utils/textProcessing.ts
  function stripHtml(html: string): string
  function splitSentences(text: string): string[]
  function extractEmojis(text: string): string[]
  function analyzeSentenceLength(sentences: string[]): Statistics
  function extractKeyPhrases(text: string): string[]
  ```
- [ ] GPT-4 기반 스타일 분석
  - 어투 분석 (격식체/비격식체)
  - 자주 사용하는 표현 추출
  - 문장 패턴 분석
  - 글 구조 분석
- [ ] 카테고리별 특징 추출
- [ ] 통계 데이터 생성
- [ ] 스타일 프로파일 생성
  ```typescript
  // services/styleAnalysis.service.ts
  class StyleAnalysisService {
    async analyzePost(post: Post): Promise<PostAnalysis>
    async analyzeBatch(posts: Post[]): Promise<BatchAnalysis>
    async buildStyleProfile(analyses: BatchAnalysis): Promise<StyleProfile>
    async generateStyleGuide(profile: StyleProfile): Promise<string>
  }
  ```
- [ ] 결과 시각화 (AI 스타일 분석 화면)

**산출물**:
- 스타일 분석 엔진
- 스타일 프로파일 데이터 구조

#### Week 7: 글 생성 엔진 - 카테고리별
**목표**: 주제 기반 블로그 글 자동 생성

**작업 내용**:
- [ ] 프롬프트 템플릿 시스템
  ```typescript
  // prompts/templates.ts
  const BLOG_POST_TEMPLATE = `
  {styleGuide}

  다음 조건으로 블로그 글을 작성해주세요:
  - 주제: {topic}
  - 카테고리: {category}
  - 목표 길이: {targetLength}자
  - 특별 요구사항: {requirements}
  `;
  ```
- [ ] 카테고리별 글 생성 서비스
  ```typescript
  // services/contentGeneration.service.ts
  class ContentGenerationService {
    async generatePost(params: GenerateParams): Promise<GeneratedPost>
    async generatePostStream(params: GenerateParams, onProgress: ProgressCallback): Promise<GeneratedPost>
    async regenerate(postId: string, feedback: string): Promise<GeneratedPost>
    async refineContent(content: string, instruction: string): Promise<string>
  }
  ```
- [ ] 실시간 생성 진행 UI
  - 진행률 표시
  - 단계별 상태 (분석중, 작성중, 완료)
- [ ] 생성 옵션 처리
  - 글자 수 조절
  - 스타일 강도 조절
  - 이모지 사용 여부
- [ ] 제목 자동 생성
- [ ] 해시태그 자동 생성
- [ ] 후처리 및 포맷팅

**산출물**:
- 카테고리별 글 작성 기능 완성
- 실시간 생성 UI

#### Week 8: 글 생성 엔진 - 사진 기반
**목표**: 이미지 분석 및 스토리텔링 생성

**작업 내용**:
- [ ] 이미지 선택 및 업로드 UI
  ```typescript
  // components/ImagePicker.tsx
  - 갤러리 선택
  - 카메라 촬영
  - 다중 선택 (최대 10장)
  - 순서 조정 (드래그 앤 드롭)
  - 미리보기
  ```
- [ ] 이미지 최적화
  - 리사이즈 (최대 2000x2000)
  - 압축 (품질 80%)
  - 포맷 변환 (JPEG)
- [ ] GPT-4 Vision 이미지 분석
  ```typescript
  // services/imageAnalysis.service.ts
  class ImageAnalysisService {
    async analyzeImage(imageUrl: string): Promise<ImageAnalysis>
    async analyzeBatch(imageUrls: string[]): Promise<ImageAnalysis[]>
    async generateCaption(analysis: ImageAnalysis): Promise<string>
  }
  ```
- [ ] 이미지 기반 스토리텔링
  - 여러 이미지를 엮은 내러티브
  - 시간 순서 또는 주제별 전개
- [ ] 이미지 설명 자동 생성
- [ ] 이미지 배치 최적화
- [ ] 사진 포스팅 전용 UI

**산출물**:
- 사진으로 포스팅 기능 완성
- 이미지 분석 및 스토리텔링 엔진

---

### Phase 3: 편집 & 발행 (3주)

#### Week 9: 리치 텍스트 에디터
**목표**: 고급 글 편집 기능

**작업 내용**:
- [ ] 에디터 라이브러리 통합
  ```bash
  npm install @10play/tentap-editor
  # or
  npm install react-native-pell-rich-editor
  ```
- [ ] 에디터 기능 구현
  - 텍스트 포맷팅 (볼드, 이탤릭, 밑줄)
  - 제목 스타일 (H1, H2, H3)
  - 리스트 (순서, 비순서)
  - 링크 삽입
  - 이미지 삽입/삭제
  - 인용문
  - 코드 블록
- [ ] 마크다운 지원
  - 마크다운 ↔ HTML 변환
  - 마크다운 미리보기
- [ ] 실시간 저장 (Auto-save)
  ```typescript
  // hooks/useAutoSave.ts
  function useAutoSave(content: string, interval: number = 3000)
  ```
- [ ] 편집 히스토리 (Undo/Redo)
- [ ] 글자 수/단어 수 카운터

**산출물**:
- 완성된 리치 텍스트 에디터
- 마크다운 지원

#### Week 10: AI 어시스턴트 기능
**목표**: 편집 중 AI 도움 기능

**작업 내용**:
- [ ] 문장 다듬기
  ```typescript
  async function refineSentence(sentence: string): Promise<string> {
    // GPT-4로 문장 개선
  }
  ```
- [ ] 문단 추가 제안
  ```typescript
  async function suggestNextParagraph(context: string): Promise<string[]> {
    // 이어질 내용 3가지 제안
  }
  ```
- [ ] 맞춤법 검사
  - 한국어 맞춤법 검사 API 연동
  - 오류 하이라이트
  - 수정 제안
- [ ] 표현 개선
  - 중복 표현 감지
  - 더 적절한 어휘 제안
- [ ] 톤 조정
  - 더 격식있게 / 더 캐주얼하게
  - 긍정적/부정적 톤 조절
- [ ] AI 어시스턴트 UI
  - 툴바 버튼
  - 컨텍스트 메뉴
  - 제안 팝업

**산출물**:
- AI 편집 어시스턴트 기능
- 향상된 편집 경험

#### Week 11: 발행 시스템
**목표**: 블로그 글 발행 및 관리

**작업 내용**:
- [ ] 발행 설정 UI
  - 즉시 발행 / 예약 발행
  - 공개 설정 (전체/비공개)
  - 카테고리 선택
  - 태그 편집
  - 댓글 허용 여부
- [ ] 발행 프로세스
  ```typescript
  // services/publishing.service.ts
  class PublishingService {
    async publish(post: Post, settings: PublishSettings): Promise<PublishResult>
    async schedulePublish(post: Post, publishAt: Date): Promise<void>
    async updatePost(postId: string, updates: Partial<Post>): Promise<void>
    async deletePost(postId: string): Promise<void>
  }
  ```
- [ ] 이미지 업로드 최적화
  - 병렬 업로드
  - 진행률 표시
  - 실패 시 재시도
- [ ] 예약 발행 시스템
  - 로컬 스케줄러
  - Background task 활용
- [ ] 발행 히스토리
  - 발행된 글 목록
  - 발행 일시, 플랫폼, 링크
  - 통계 (조회수, 댓글 수) - 가능한 경우
- [ ] 발행 후 알림
- [ ] 에러 처리 및 재시도

**산출물**:
- 완성된 발행 시스템
- 히스토리 관리 기능

---

### Phase 4: 고도화 & 최적화 (3주)

#### Week 12: 성능 최적화
**목표**: 앱 속도 및 안정성 향상

**작업 내용**:
- [ ] API 호출 최적화
  - React Query 캐싱 전략
  - Stale-while-revalidate 패턴
  - Prefetching
- [ ] 이미지 최적화
  - react-native-fast-image 사용
  - 이미지 캐싱
  - Lazy loading
  - Placeholder 이미지
- [ ] 번들 크기 최적화
  - Code splitting
  - Tree shaking
  - 불필요한 라이브러리 제거
- [ ] 렌더링 최적화
  - React.memo 적용
  - useMemo, useCallback 활용
  - FlatList 최적화 (VirtualizedList)
- [ ] 로딩 시간 단축
  - Splash screen 최적화
  - 초기 데이터 로딩 최적화
- [ ] 메모리 관리
  - 메모리 누수 점검
  - 이미지 메모리 최적화
- [ ] 오프라인 모드
  - 네트워크 상태 감지
  - 오프라인 큐 (글 발행 대기)
  - 로컬 우선 데이터 로드

**산출물**:
- 최적화된 앱 성능
- 오프라인 지원

#### Week 13: UX 개선
**목표**: 사용자 경험 향상

**작업 내용**:
- [ ] 온보딩 튜토리얼
  - 첫 실행 시 가이드
  - 주요 기능 소개
  - 스킵 가능
- [ ] 로딩 애니메이션
  - Skeleton screens
  - Shimmer 효과
  - 진행률 표시
- [ ] 마이크로 인터랙션
  - 버튼 피드백
  - 전환 애니메이션 (React Native Reanimated)
  - 제스처 지원 (스와이프, 드래그)
- [ ] 에러 처리
  - 사용자 친화적 에러 메시지
  - 에러 복구 제안
  - 에러 리포팅 (Sentry)
- [ ] 빈 상태 디자인
  - 글이 없을 때
  - 검색 결과 없을 때
  - 네트워크 오류 시
- [ ] 접근성 (Accessibility)
  - Screen reader 지원
  - 색상 대비 개선
  - 터치 타겟 크기
- [ ] 다크 모드
  - 라이트/다크 테마 전환
  - 시스템 설정 따르기
- [ ] 푸시 알림
  - 글 생성 완료 알림
  - 예약 발행 알림
  - 동기화 완료 알림

**산출물**:
- 향상된 UX
- 접근성 개선
- 알림 시스템

#### Week 14: 테스트 & 버그 수정
**목표**: 품질 보증 및 안정화

**작업 내용**:
- [ ] 단위 테스트
  ```typescript
  // __tests__/services/contentGeneration.test.ts
  describe('ContentGenerationService', () => {
    it('should generate post with correct structure', async () => {
      // ...
    });
  });
  ```
  - 서비스 로직 테스트
  - 유틸리티 함수 테스트
  - 커버리지 70% 이상 목표
- [ ] 통합 테스트
  - API 연동 테스트
  - 발행 플로우 테스트
  - 스타일 학습 파이프라인 테스트
- [ ] E2E 테스트 (Detox)
  ```typescript
  // e2e/blogPost.e2e.ts
  describe('Blog Post Creation', () => {
    it('should create and publish a post', async () => {
      await element(by.id('create-post-btn')).tap();
      // ...
    });
  });
  ```
  - 주요 사용자 플로우 테스트
  - 회귀 테스트
- [ ] 베타 테스트
  - TestFlight (iOS) / Internal Testing (Android)
  - 10-20명 테스터 모집
  - 피드백 수집 및 반영
- [ ] 버그 픽스
  - 크래시 수정
  - UI 버그 수정
  - 로직 오류 수정
- [ ] 성능 프로파일링
  - React DevTools Profiler
  - Flipper를 통한 네트워크 모니터링
  - 병목 지점 개선

**산출물**:
- 안정적인 앱
- 테스트 커버리지 70%+
- 베타 테스트 완료

---

### Phase 5: 출시 준비 (2주)

#### Week 15: 출시 준비
**목표**: 앱스토어/플레이스토어 출시 준비

**작업 내용**:
- [ ] 앱 아이콘 및 스플래시 스크린 제작
  - 여러 크기 준비 (iOS, Android)
  - 앱 아이콘 가이드라인 준수
- [ ] 스크린샷 준비
  - 주요 화면 5-8개
  - 다양한 디바이스 크기
  - 설명 텍스트 오버레이
- [ ] 앱 설명 작성
  - 한국어, 영어
  - 주요 기능 강조
  - 키워드 최적화
- [ ] 개인정보 처리방침 작성
  ```
  수집하는 정보:
  - 이메일 주소
  - 블로그 OAuth 토큰
  - 작성한 글 (로컬 저장)
  - OpenAI API 키 (사용자가 직접 입력)

  제3자 제공:
  - OpenAI (글 생성 목적)
  - 블로그 플랫폼 (발행 목적)
  ```
- [ ] 이용약관 작성
- [ ] App Store 등록
  - Apple Developer 계정
  - App Store Connect 설정
  - 앱 정보 입력
  - 빌드 업로드 (Xcode)
- [ ] Google Play Console 등록
  - Google Play Console 계정
  - 앱 정보 입력
  - APK/AAB 업로드
- [ ] 심사 준비
  - 테스트 계정 제공
  - 심사 가이드라인 점검
  - 거부 사유 사전 대응

**산출물**:
- 출시 준비 완료
- 스토어 등록 완료

#### Week 16: 최종 QA 및 출시
**목표**: 최종 점검 및 출시

**작업 내용**:
- [ ] 최종 QA
  - 모든 주요 기능 점검
  - 다양한 디바이스 테스트
  - 네트워크 상황별 테스트
  - 에지 케이스 테스트
- [ ] 충돌 보고 시스템 확인
  - Sentry 연동 확인
  - 크래시 리포트 테스트
- [ ] 분석 도구 확인
  - Firebase Analytics 이벤트 추적
  - 주요 메트릭 정의
- [ ] 출시 전 체크리스트
  - [ ] 모든 API 키 환경변수 확인
  - [ ] 프로덕션 빌드 테스트
  - [ ] 개인정보 처리방침 링크 확인
  - [ ] 앱 버전 정보 확인
  - [ ] 크래시 리포팅 작동 확인
  - [ ] 푸시 알림 작동 확인
- [ ] 소프트 론치
  - 한국에서 먼저 출시
  - 초기 사용자 피드백 수집
- [ ] 모니터링 시스템 가동
  - 실시간 크래시 모니터링
  - 사용자 피드백 수집
  - 리뷰 모니터링
- [ ] 핫픽스 준비
  - 긴급 패치 프로세스 확립
  - Fast lane 설정

**산출물**:
- 정식 출시
- 모니터링 시스템 가동

---

## API 명세

### 1. Blog API (Internal)

#### 1.1 블로그 연동
```typescript
POST /api/blog/connect
Request:
{
  platform: 'tistory' | 'naver' | 'velog',
  authCode: string
}

Response:
{
  success: boolean,
  connection: {
    id: string,
    platform: string,
    blogName: string,
    blogUrl: string,
    accessToken: string,  // 암호화됨
    connectedAt: Date
  }
}
```

#### 1.2 글 목록 조회
```typescript
GET /api/blog/posts?page=1&limit=30&platform=tistory

Response:
{
  posts: [
    {
      id: string,
      title: string,
      content: string,
      category: string,
      tags: string[],
      publishedAt: Date,
      url: string
    }
  ],
  pagination: {
    page: number,
    limit: number,
    total: number,
    hasNext: boolean
  }
}
```

#### 1.3 글 발행
```typescript
POST /api/blog/publish
Request:
{
  platform: 'tistory' | 'naver' | 'velog',
  post: {
    title: string,
    content: string,  // HTML
    category: string,
    tags: string[],
    visibility: 'public' | 'private',
    publishAt?: Date  // 예약 발행
  }
}

Response:
{
  success: boolean,
  postId: string,
  url: string,
  publishedAt: Date
}
```

### 2. AI API (Internal)

#### 2.1 스타일 분석
```typescript
POST /api/ai/analyze-style
Request:
{
  posts: [
    {
      title: string,
      content: string,
      category: string
    }
  ]
}

Response:
{
  styleProfile: StyleProfile,
  analysisId: string,
  analyzedAt: Date
}
```

#### 2.2 글 생성
```typescript
POST /api/ai/generate-post
Request:
{
  type: 'category' | 'photo',
  topic?: string,
  category: string,
  images?: string[],  // URLs
  options: {
    targetLength: number,
    styleIntensity: number,
    useEmoji: boolean,
    autoHashtag: boolean
  }
}

Response (Streaming):
{
  chunk: string,
  progress: number,
  stage: 'analyzing' | 'generating' | 'complete'
}

Final Response:
{
  post: {
    title: string,
    content: string,
    hashtags: string[],
    wordCount: number
  },
  metadata: {
    generatedAt: Date,
    model: string,
    tokensUsed: number
  }
}
```

#### 2.3 이미지 분석
```typescript
POST /api/ai/analyze-image
Request:
{
  imageUrl: string
}

Response:
{
  analysis: {
    objects: string[],
    scene: string,
    mood: string,
    colors: string[],
    description: string
  }
}
```

### 3. OpenAI API (External)

#### 3.1 Chat Completions
```typescript
POST https://api.openai.com/v1/chat/completions
Headers:
{
  "Authorization": "Bearer ${OPENAI_API_KEY}",
  "Content-Type": "application/json"
}

Request:
{
  model: "gpt-4-turbo-preview",
  messages: [
    {
      role: "system",
      content: "당신은 블로그 작성을 돕는 AI입니다..."
    },
    {
      role: "user",
      content: "제주도 여행에 대한 블로그 글을 작성해주세요"
    }
  ],
  temperature: 0.7,
  max_tokens: 2000,
  stream: true  // 스트리밍 응답
}

Response (Stream):
data: {"id":"chatcmpl-xxx","object":"chat.completion.chunk","choices":[{"delta":{"content":"안녕"},"index":0}]}
data: {"id":"chatcmpl-xxx","object":"chat.completion.chunk","choices":[{"delta":{"content":"하세요"},"index":0}]}
...
```

#### 3.2 Vision
```typescript
POST https://api.openai.com/v1/chat/completions
Request:
{
  model: "gpt-4-vision-preview",
  messages: [
    {
      role: "user",
      content: [
        {
          type: "text",
          text: "이 이미지를 설명해주세요"
        },
        {
          type: "image_url",
          image_url: {
            url: "https://example.com/image.jpg"
          }
        }
      ]
    }
  ],
  max_tokens: 500
}
```

### 4. Tistory API (External)

#### 4.1 OAuth
```
GET https://www.tistory.com/oauth/authorize
  ?client_id={client_id}
  &redirect_uri={redirect_uri}
  &response_type=code

POST https://www.tistory.com/oauth/access_token
  ?client_id={client_id}
  &client_secret={client_secret}
  &redirect_uri={redirect_uri}
  &code={code}
  &grant_type=authorization_code
```

#### 4.2 포스트 작성
```
POST https://www.tistory.com/apis/post/write
  ?access_token={token}
  &output=json
  &blogName={blog_name}
  &title={title}
  &content={content}
  &visibility=3
  &category={category_id}
  &tag={tags}
```

---

## 데이터베이스 설계

### Firestore Collections

#### users
```typescript
/users/{userId}
{
  uid: string,
  email: string,
  displayName: string,
  photoURL: string,
  createdAt: Timestamp,
  lastLoginAt: Timestamp,
  settings: {
    theme: 'light' | 'dark' | 'system',
    language: 'ko' | 'en',
    notifications: {
      postComplete: boolean,
      publishSuccess: boolean,
      syncComplete: boolean
    },
    defaultCategory: string,
    autoSave: boolean
  }
}
```

#### blogConnections
```typescript
/blogConnections/{userId}/connections/{connectionId}
{
  id: string,
  platform: 'tistory' | 'naver' | 'velog',
  blogName: string,
  blogUrl: string,
  accessToken: string,  // 암호화
  refreshToken: string,  // 암호화
  tokenExpiresAt: Timestamp,
  connectedAt: Timestamp,
  lastSyncAt: Timestamp,
  isActive: boolean,
  metadata: {
    blogId: string,
    userId: string,
    nickname: string
  }
}
```

#### styleProfiles
```typescript
/styleProfiles/{userId}
{
  userId: string,
  statistics: {
    totalPosts: number,
    avgPostLength: number,
    avgSentenceLength: number,
    emojiUsageRate: number,
    postingFrequency: number
  },
  linguisticStyle: {
    tone: string,
    perspective: string,
    commonPhrases: string[],
    sentencePatterns: string[],
    vocabularyLevel: string
  },
  structuralPattern: {
    introStyle: string,
    bodyDevelopment: string,
    conclusionStyle: string,
    avgParagraphCount: number
  },
  categoryStyles: {
    [category: string]: {
      samplePosts: string[],
      specificPhrases: string[],
      avgLength: number
    }
  },
  styleGuide: string,  // GPT 프롬프트용
  lastUpdated: Timestamp,
  analyzedPostCount: number,
  dataRange: {
    from: Timestamp,
    to: Timestamp
  }
}
```

#### posts (drafts)
```typescript
/posts/{userId}/drafts/{postId}
{
  id: string,
  title: string,
  content: string,  // Markdown or HTML
  category: string,
  hashtags: string[],
  images: [
    {
      url: string,
      caption: string,
      order: number
    }
  ],
  metadata: {
    type: 'category' | 'photo',
    topic: string,
    generatedAt: Timestamp,
    model: string,
    tokensUsed: number
  },
  status: 'draft' | 'editing' | 'ready',
  wordCount: number,
  createdAt: Timestamp,
  updatedAt: Timestamp,
  autoSavedAt: Timestamp
}
```

#### publishHistory
```typescript
/publishHistory/{userId}/published/{historyId}
{
  id: string,
  postId: string,
  platform: string,
  blogPostId: string,  // 블로그 플랫폼의 포스트 ID
  title: string,
  url: string,
  category: string,
  hashtags: string[],
  publishedAt: Timestamp,
  scheduledAt: Timestamp,  // 예약 발행인 경우
  status: 'published' | 'failed' | 'scheduled',
  error: string,  // 실패 시
  statistics: {
    views: number,
    comments: number,
    likes: number
  }  // 플랫폼이 지원하는 경우
}
```

#### apiUsage (비용 추적)
```typescript
/apiUsage/{userId}/usage/{month}
{
  userId: string,
  month: string,  // "2024-01"
  openai: {
    totalRequests: number,
    totalTokens: number,
    gpt4Tokens: number,
    gpt4VisionTokens: number,
    estimatedCost: number
  },
  blog: {
    totalRequests: number,
    byPlatform: {
      tistory: number,
      naver: number
    }
  },
  updatedAt: Timestamp
}
```

### Local Storage (AsyncStorage / SQLite)

#### LocalDrafts (SQLite)
```sql
CREATE TABLE drafts (
  id TEXT PRIMARY KEY,
  title TEXT,
  content TEXT,
  category TEXT,
  hashtags TEXT,  -- JSON array
  images TEXT,    -- JSON array
  created_at INTEGER,
  updated_at INTEGER,
  auto_saved_at INTEGER,
  synced BOOLEAN DEFAULT 0
);

CREATE INDEX idx_updated_at ON drafts(updated_at);
```

#### Settings (AsyncStorage)
```typescript
{
  "@blogtwin:theme": "light" | "dark" | "system",
  "@blogtwin:openai_api_key": "sk-xxx",  // 암호화
  "@blogtwin:default_category": "일상",
  "@blogtwin:last_sync": "2024-01-15T10:30:00Z"
}
```

---

## 보안 및 개인정보

### 1. 데이터 보호

#### 1.1 민감 정보 암호화
```typescript
// utils/encryption.ts
import CryptoJS from 'crypto-js';
import { ENCRYPTION_KEY } from '@env';

export function encryptToken(token: string): string {
  return CryptoJS.AES.encrypt(token, ENCRYPTION_KEY).toString();
}

export function decryptToken(encrypted: string): string {
  const bytes = CryptoJS.AES.decrypt(encrypted, ENCRYPTION_KEY);
  return bytes.toString(CryptoJS.enc.Utf8);
}
```

**암호화 대상**:
- OAuth Access Token
- OAuth Refresh Token
- OpenAI API Key
- 기타 민감 정보

#### 1.2 통신 보안
- 모든 API 통신 HTTPS 사용
- Certificate Pinning (선택사항)
- Request/Response 로깅 시 민감 정보 마스킹

### 2. 개인정보 수집 및 이용

#### 2.1 수집하는 정보
- **필수**: 이메일 주소, OAuth 토큰
- **선택**: 블로그 글 내용 (분석 목적)
- **자동 수집**: 앱 사용 로그, 오류 로그

#### 2.2 제3자 제공
- **OpenAI**: 글 생성을 위한 프롬프트 (개인식별 정보 제외)
- **블로그 플랫폼**: 발행할 글 내용
- **Firebase**: 앱 데이터 저장 및 분석

#### 2.3 데이터 보관 및 삭제
- 사용자 계정 삭제 시 모든 데이터 삭제
- 임시 저장 글 30일 후 자동 삭제 (옵션)
- 발행 히스토리 1년 보관 후 삭제

### 3. 권한 관리

#### 3.1 필요 권한
```xml
<!-- Android -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.CAMERA" />
```

```xml
<!-- iOS Info.plist -->
<key>NSPhotoLibraryUsageDescription</key>
<string>블로그 포스팅에 사용할 사진을 선택하기 위해 필요합니다.</string>

<key>NSCameraUsageDescription</key>
<string>블로그 포스팅에 사용할 사진을 촬영하기 위해 필요합니다.</string>
```

#### 3.2 권한 요청 타이밍
- 사진 선택 시점에 갤러리 권한 요청
- 카메라 촬영 시점에 카메라 권한 요청
- 미리 요청하지 않고 필요 시점에만 요청

---

## 배포 전략

### 1. 버전 관리

#### Semantic Versioning
- **Major (1.x.x)**: 큰 변경사항, 하위 호환성 없음
- **Minor (x.1.x)**: 새로운 기능 추가
- **Patch (x.x.1)**: 버그 수정

#### 초기 버전
- v0.1.0: 내부 알파 테스트
- v0.9.0: 베타 테스트
- v1.0.0: 정식 출시

### 2. CI/CD 파이프라인

#### GitHub Actions
```yaml
# .github/workflows/build.yml
name: Build and Test

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm test
      - name: Lint
        run: npm run lint

  build-ios:
    runs-on: macos-latest
    needs: test
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Build iOS
        run: npx react-native build-ios --mode Release

  build-android:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v3
      - name: Build Android
        run: cd android && ./gradlew assembleRelease
```

### 3. 배포 단계

#### 3.1 개발 환경
- 브랜치: `develop`
- 자동 빌드 및 테스트
- 개발자 디바이스 테스트

#### 3.2 스테이징
- 브랜치: `staging`
- TestFlight / Internal Testing
- QA 팀 테스트

#### 3.3 프로덕션
- 브랜치: `main`
- App Store / Google Play
- 단계적 배포 (10% → 50% → 100%)

### 4. 모니터링

#### 4.1 크래시 리포팅
```typescript
import * as Sentry from '@sentry/react-native';

Sentry.init({
  dsn: 'https://xxx@sentry.io/xxx',
  environment: __DEV__ ? 'development' : 'production',
  enableAutoSessionTracking: true,
  tracesSampleRate: 0.2
});
```

#### 4.2 분석
```typescript
import analytics from '@react-native-firebase/analytics';

// 이벤트 추적
await analytics().logEvent('post_generated', {
  category: 'travel',
  length: 1500,
  type: 'category'
});

// 화면 추적
await analytics().logScreenView({
  screen_name: 'PostEditor',
  screen_class: 'PostEditorScreen'
});
```

#### 4.3 주요 메트릭
- DAU (Daily Active Users)
- 글 생성 완료율
- 발행 성공률
- 평균 글 생성 시간
- 크래시율
- API 응답 시간

---

## 향후 로드맵 (v1.1+)

### 단기 (v1.1 - v1.3)
- [ ] 더 많은 블로그 플랫폼 지원 (Medium, Notion 등)
- [ ] AI 모델 선택 기능 (GPT-4, Claude, Gemini)
- [ ] 글 스타일 커스터마이징 (더 유머러스하게, 전문적으로 등)
- [ ] 태블릿 최적화
- [ ] 웹 버전 출시

### 중기 (v2.0)
- [ ] 협업 기능 (팀 블로그 관리)
- [ ] SEO 최적화 제안
- [ ] 글 성과 분석 (조회수, 체류시간 추적)
- [ ] 예약 발행 캘린더
- [ ] 멀티 블로그 동시 발행

### 장기 (v3.0+)
- [ ] 커뮤니티 기능 (템플릿 공유)
- [ ] 수익화 대시보드
- [ ] 음성으로 글 작성
- [ ] 영상 → 블로그 글 변환
- [ ] 다국어 자동 번역 및 발행

---

## 예상 비용

### 개발 비용
- **OpenAI API**: $50-200/월 (테스트 및 초기 사용자)
- **Firebase**: Free tier → Blaze ($25-100/월, 사용자 증가 시)
- **Apple Developer**: $99/년
- **Google Play**: $25 (일회성)
- **기타 서비스**: $20-50/월

### 운영 비용 (사용자 1000명 기준)
- **OpenAI API**: $500-1000/월
- **Firebase**: $200-500/월
- **모니터링 (Sentry 등)**: $50/월
- **총 예상**: $750-1550/월

---

## 성공 지표

### 론칭 후 3개월
- [ ] 다운로드 1000건
- [ ] MAU 500명
- [ ] 글 생성 완료율 80%+
- [ ] 발행 성공률 95%+
- [ ] 평균 평점 4.0+

### 론칭 후 6개월
- [ ] 다운로드 5000건
- [ ] MAU 2000명
- [ ] 리텐션 30% (D30)
- [ ] 평균 평점 4.5+

---

## 리스크 관리

### 기술적 리스크
| 리스크 | 영향 | 대응 방안 |
|--------|------|-----------|
| OpenAI API 장애 | 높음 | 대체 모델 준비 (Claude, Gemini) |
| 블로그 API 변경 | 중간 | 버전별 대응, 에러 핸들링 강화 |
| 앱 크래시 | 높음 | 철저한 테스트, 모니터링 |

### 비즈니스 리스크
| 리스크 | 영향 | 대응 방안 |
|--------|------|-----------|
| 낮은 사용자 유입 | 높음 | 마케팅 강화, 무료 체험 제공 |
| API 비용 폭증 | 중간 | 사용량 제한, 프리미엄 플랜 |
| 경쟁 서비스 등장 | 중간 | 차별화 기능 지속 개발 |

---

## 팀 구성 (권장)

### 최소 구성 (1-2인)
- **풀스택 개발자**: 앱 개발 + 백엔드

### 이상적 구성 (3-4인)
- **모바일 개발자**: React Native 앱 개발
- **백엔드 개발자**: API, Firebase 관리
- **AI/ML 엔지니어**: 프롬프트 최적화, 스타일 학습
- **디자이너**: UI/UX 디자인

---

## 참고 자료

### 공식 문서
- [React Native Docs](https://reactnative.dev/)
- [OpenAI API Docs](https://platform.openai.com/docs)
- [Tistory API Docs](https://tistory.github.io/document-tistory-apis/)
- [Firebase Docs](https://firebase.google.com/docs)

### 커뮤니티
- React Native Community
- OpenAI Developer Forum
- 블로그 개발자 커뮤니티

---

**문서 버전**: 1.0
**최종 수정일**: 2024-01-15
**작성자**: BlogTwin Team
